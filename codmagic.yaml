workflows:
  android-workflow:
    name: STL Manager Android

    environment:
      flutter: stable
      java: 17

    scripts:
      - flutter clean
      - flutter pub get
      - flutter build apk --release --split-per-abi

    artifacts:
      - build//outputs//*.apk

    publishing:
      email:
        recipients:
          - hmoodyking888@gmail.com